# Storage Advisor — backend dependencies
#
# Phase 1 needs almost nothing: sqlite3 and os.scandir are in the Python
# standard library, so the only real dependency is the test runner.
#
# Install into a virtual environment:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r backend/requirements.txt
#
# Pins use `~=` (compatible-release): patch/minor updates allowed, majors held.

pytest~=8.2
